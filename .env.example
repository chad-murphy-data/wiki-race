# PartyKit host for client connection. In production, set this to the URL
# printed by `npx partykit deploy` (without the scheme), e.g.
#   wiki-race.your-username.partykit.dev
# For local dev, omit this — the client falls back to 127.0.0.1:1999.
NEXT_PUBLIC_PARTYKIT_HOST=
